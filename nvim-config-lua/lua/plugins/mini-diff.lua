-- Define the mini-diff plugin
return {
	"echasnovski/mini.diff",
	version = "*", -- Use the latest version
	config = function()
		-- Optional: Add any specific configuration for mini-diff here
		-- For example, you might want to set up key mappings or other settings
	end,
}
